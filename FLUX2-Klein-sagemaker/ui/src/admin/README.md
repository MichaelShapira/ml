# admin/

Admin-only UI (visible when `isAdmin`).

Implemented in later tasks:

- `EndpointPanel.tsx` — endpoint list/status/start/stop (task 11.1)
- `ScheduleCalendar.tsx` — working-hours calendar (task 11.2)
- `AdminTab.tsx` — admin tab gating + wiring (task 11.3)
