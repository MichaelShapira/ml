# auth/

Amplify Auth wrapper and `useAuth` hook (Cognito).

Implemented in task 10.1. Exposes `isAuthenticated` and `isAdmin` (derived from
the Cognito `profile` claim) and handles sign-in/sign-out.
