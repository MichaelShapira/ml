# api/

Typed fetch wrapper (`client.ts`) that attaches the Cognito ID token as
`Authorization: Bearer` and exposes typed calls for the generation submit/poll
and admin endpoint/schedule routes.

Implemented in task 10.2.
