# booth/

Capture-flow components and the pure state machine (`machine.ts`).

Implemented in later tasks:

- `machine.ts` — capture-flow state machine (task 4.1)
- `StartScreen.tsx`, `CameraView.tsx` (task 10.3)
- `ReviewScreen.tsx`, `EffectSelector.tsx` (task 10.4)
- `LoadingScreen.tsx`, `ResultScreen.tsx` (task 10.5)
