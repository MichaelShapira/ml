# Local dev only (npm run dev). In production these come from config.js, which
# deploy.sh writes from CDK stack outputs. Copy to .env.local and fill in.
VITE_REGION=us-east-1
VITE_USER_POOL_ID=us-east-1_xxxxxxxxx
VITE_USER_POOL_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
VITE_API_URL=https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com/prod/
